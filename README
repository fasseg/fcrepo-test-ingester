run this test tool by building it and issueing a commandline like:

$ mvn package
$ java -jar test-ingester-0.0.1-SNAPSHOT.jar http://localhost:8080/fedora/ fedoraAdmin fed 1000 500000

the command line syntax for the executable class is as following:
java -jar test-ingester-0.0.1-SNAPSHOT.jar <host> <user> <pass> <num> <size>

host = the fedora host to write to
user = the fedora username
pass = the fedora password
num = the number of datastreams to create
size = the size of the individual created datastreams

